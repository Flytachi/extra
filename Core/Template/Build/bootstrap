<?php

// include Extra
require 'extra/warframe.php';

Warframe::loader();

if (Warframe::$cfg['GLOBAL_SETTING']['DEBUG']) {
    ini_set('error_reporting', E_ALL);
    ini_set('display_errors', 1);
    ini_set('display_startup_errors', 1);
}

// run Route
\Extra\Src\Route::start();

?>