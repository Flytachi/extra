<?php

function isPermission(string $name): bool
{
    if (isAdmin()) return true;
    else {
        importRepository('UserPermissionRepository');
        return (new UserPermissionRepository)->getPermission($name); 
    }
}

?>